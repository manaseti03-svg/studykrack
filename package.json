{
  "name": "studykrack-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "python backend/main.py",
    "dev": "npm run dev:frontend",
    "install:all": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "start": "npm run dev"
  }
}
